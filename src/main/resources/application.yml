spring:
  datasource:
    url: jdbc:mysql://192.168.0.67:3306/msy?allowPublicKeyRetrieval=true&useSSL=false
    username: pjuser
    password: 11111111
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

# ? JWT ?? ??
jwt:
  secret: qwerty1234567890qwerty1234567890abcd  # 32? ?? ??
